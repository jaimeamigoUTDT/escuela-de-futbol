class Cancha {
    constructor({ cancha_id, address, size, shoe_type, buffet_available, parking_available }) {
      this.cancha_id = cancha_id;
      this.address = address;
      this.size = size;
      this.shoe_type = shoe_type;
      this.buffet_available = buffet_available;
      this.parking_available = parking_available;
    }
  }

export default Cancha;